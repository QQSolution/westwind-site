# West Wind Logistics — driver recruiting site

React + Vite + TypeScript + Tailwind. Light, Apple/Tesla/GP-Transco-grade.
All copy & data live in `src/content/site.ts`. Media slots use `MediaPlaceholder`.
DRAFT: placeholder pay (`config.DRAFT=true`), demo lead mode, placeholder media.

```
bun install && bun run dev
```
